package com.android.testapp.data.database.dao

import androidx.room.*

@Dao
interface CollectionDao {

   /* @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertAll(contents: List<Collection>)

    @Query("SELECT * from Collection")
    fun getAllCatalogue() : LiveData<List<Collection>>

   */

}